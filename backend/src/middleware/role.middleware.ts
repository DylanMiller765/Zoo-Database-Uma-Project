import { Request, Response, NextFunction } from 'express';

// TODO: Implement requireRole middleware

// TODO: Implement requirePermission middleware

// TODO: Export convenience middleware (requireEmployee, requireManager, etc.)
